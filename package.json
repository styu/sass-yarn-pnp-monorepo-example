{
  "name": "sass-yarn-pnp-monorepo-example",
  "packageManager": "yarn@4.4.1",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "assemble": "turbo run assemble",
    "build": "turbo run build",
    "build-postcss": "turbo run build-postcss",
    "lint": "turbo run lint",
    "lint-fix": "turbo run lint-fix",
    "clean": "turbo run clean"
  },
  "dependencies": {
    "prettier": "^3.3.3",
    "turbo": "^2.0.14",
    "typescript": "~5.5.3"
  },
  "engines": {
    "node": ">=20.9.0"
  }
}
